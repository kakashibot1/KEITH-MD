name: KEITH-MD Bot Auto Deploy (30min)

on:
  push:
    branches:
      - main

  workflow_dispatch:

  schedule:
    - cron: "*/30 * * * *"   # ⏰ toutes les 30 minutes

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 25      # le job s’arrête avant le prochain cron

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      - name: 🟢 Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Installer les dépendances
        run: npm install

      - name: ▶️ Lancer le bot
        run: npm start
