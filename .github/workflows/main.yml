name: KEITH-MD Bot Auto Deploy (4h)

on:
  push:
    branches:
      - main

  workflow_dispatch:

  schedule:
    - cron: "0 */4 * * *"   # ⏰ toutes les 4 heures

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 235    # ~3h55 (avant le prochain redéploiement)

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v4

      - name: 🟢 Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Installer les dépendances
        run: npm install

      - name: ▶️ Lancer le bot
        run: npm start
